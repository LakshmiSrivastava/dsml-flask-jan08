name: First Test on AWS

on:
  push:
    branches:
      - main


jobs:

  job1:
    runs-on: ubuntu-latest # macos-latest, windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4 # some of the inbuilt functions, provided by github for doing some common tasks. checkout means make a copy of the code from the repo

      - name: Install Python-3
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run tests
        run: pytest

  job2:
    needs: job1
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Login to DockerHub
        uses: docker/login-action@v3 # this action logins to dockerhub
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build Docker Image and push to DockerHub
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: shivam13juna/second-action:latest

  #job3:
  #  needs: job1
  #  runs-on: ubuntu-latest
  #  steps:
  #    - uses: actions/checkout@v4

  #    - name: Configure AWS credentials
  #      uses: aws-actions/configure-aws-credentials@v1
  #      with:
  #        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #        aws-region: us-east-1

  #    - name: Login to Amazon ECR
  #      id: login-ecr
  #      uses: aws-actions/amazon-ecr-login@v2

  #    - name: Build, tag, and push image to Amazon ECR
  #      id: build-image
  #      env:
  #        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
  #        ECR_REPOSITORY: jan22_ecr_flask
  #        IMAGE_TAG: ${{ github.sha }}
  #      run: |
  #        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
  #        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
  #        echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

  #    - name: Fill in the new image ID in the Amazon ECS task definition
  #      id: task-def
  #      uses: aws-actions/amazon-ecs-render-task-definition@v1
  #      with:
  #        task-definition: task-flask-jan22-2025-revision1.json
  #        container-name: flask-container-jan22-2025
  #        image: ${{ steps.build-image.outputs.image }}

  #    - name: Deploy Amazon ECS task definition
  #      uses: aws-actions/amazon-ecs-deploy-task-definition@v2
  #      with:
  #        task-definition: ${{ steps.task-def.outputs.task-definition }}
  #        service: flask-service-jan22-2025
  #        cluster: flask-cluster-jan22-2025
  #        wait-for-service-stability: true



    
          

      
    